<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Play -->
    <item
        android:id="@+id/nav_play"
        android:icon="@drawable/play_button_xml"
        tools:ignore="MenuTitle" />

    <!-- Upgrades -->
    <item
        android:id="@+id/nav_upgrades"
        android:icon="@drawable/rectangle_with_arrow"
        tools:ignore="MenuTitle" />

    <!-- Rebirth -->
    <item
        android:id="@+id/nav_rebirth"
        android:icon="@drawable/circle_with_arrow"
        tools:ignore="MenuTitle" />


    <!-- Settings -->
    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/options_button_xml"
        tools:ignore="MenuTitle"
        android:checkable="false"/>

</menu>
